import { describe, expect, it } from 'vitest'

// Trivial smoke test proving the Vitest harness runs. Removed in a later task.
describe('smoke', () => {
  it('arithmetic works', () => {
    expect(1 + 1).toBe(2)
  })
})
