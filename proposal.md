# 系統監控儀表板 — 需求文件 (Proposal)

> 文件版本：v1.0　|　建立日期：2026-06-03
> 本文件根據需求訪談逐項確認而成，作為後續開發的依據。

---

## 1. 專案目標

打造一個**單機系統監控儀表板**，供個人實際使用。

- **Backend**：以 **Rust** 撰寫的可執行檔（binary），負責定期收集本機系統指標、存入資料庫，並透過 HTTP API 對外提供資料。
- **Frontend**：獨立的 **Web 應用程式**，定期向 Backend API 拉取（poll）資料，使用乾淨的視覺化呈現即時與歷史走勢圖表。

一句話描述：**「在自己的電腦上跑一個小服務，用瀏覽器（含手機/平板）就能看到這台機器的 CPU、記憶體、網路即時與最近 7 天的走勢圖。」**

---

## 2. 需求訪談確認結果

以下為與使用者逐項確認的結論，作為所有設計決策的根據：

| 項目 | 決定 |
|------|------|
| 監控對象 | 本機系統資源 |
| 監控範圍 | **單一台機器**（不做多主機 / agent 架構） |
| 專案用途 | 個人實際使用（需穩定可用，但非商用） |
| 監控指標 | **CPU 使用率、記憶體 (RAM)、網路流量**（本版不含磁碟） |
| 歷史資料 | **保留 7 天**，逾期自動刪除 |
| 圖表更新頻率 | **每 10 秒以上**（採 10 秒） |
| 存取對象 | 同網路其他裝置（手機 / 平板）也可瀏覽 |
| 密碼保護 | **不需要**（信任自家網路） |
| 前端服務方式 | 前後端**分開兩個服務** |
| 前端技術 | **React + Vite + TailwindCSS + Chart.js** |
| 門檻警示功能 | **不需要**（本版先做核心，未來可擴充） |

---

## 3. 功能需求 (Functional Requirements)

### 3.1 監控指標

本版本收集並呈現以下三類指標：

1. **CPU 使用率**
   - 整體 CPU 使用百分比 (%)。
   - （加分項，非必要）各核心的使用率。

2. **記憶體 (RAM)**
   - 已使用 / 可用 / 總量（單位 MB 或 GB）。
   - 以百分比與絕對值兩種方式呈現。

3. **網路流量**
   - 即時上傳 / 下載速率（如 KB/s、MB/s）。
   - 由 Backend 以「兩次取樣的累計位元組差 ÷ 時間差」換算速率。

> 明確排除：本版**不監控磁碟**、不監控特定應用程式、不做多主機。

### 3.2 資料收集與保留

- Backend 啟動後，於背景**每 10 秒取樣一次**所有指標，寫入本機資料庫。
- 資料保留 **7 天**；Backend 需定期清除超過 7 天的舊資料（例如每小時執行一次清理），避免資料庫無限增長。

### 3.3 API（Backend 對外介面）

Backend 提供 HTTP JSON API，至少包含：

- **取得目前即時狀態**
  - 例：`GET /api/metrics/current`
  - 回傳最新一筆 CPU / RAM / 網路數值。

- **取得歷史走勢**
  - 例：`GET /api/metrics/history?range=1h`
  - 支援以時間範圍查詢（如最近 1 小時、6 小時、24 小時、7 天）。
  - 回傳時間序列陣列，供前端畫折線圖。

- **健康檢查（選配）**
  - 例：`GET /api/health` 回傳服務存活狀態。

> 為支援前後端分離，API 需開啟 **CORS**，允許前端來源存取。

### 3.4 前端呈現

- 前端為單頁應用，**每 10 秒**向 Backend 拉取一次最新資料更新畫面。
- 至少呈現：
  - CPU、RAM、網路的**目前數值卡片**（大數字一眼可見）。
  - CPU、RAM、網路的**歷史折線圖**（Chart.js）。
  - 可切換歷史時間範圍（例如 1 小時 / 24 小時 / 7 天）。
- 視覺風格以 **TailwindCSS** 維持乾淨、簡潔、易讀。
- 需在桌機與手機瀏覽器皆能正常顯示（基本 RWD）。

---

## 4. 非功能需求 (Non-Functional Requirements)

- **存取範圍**：Backend 與前端皆綁定 `0.0.0.0`，讓同一區網內的其他裝置可透過本機 IP 存取。
- **安全性**：本版**不設密碼 / 登入**（信任家用網路）。
  - ⚠️ 注意：因此**請勿**將此服務直接暴露到公開網際網路。
- **效能**：10 秒取樣、單機、資料量小，資源占用應極低。
- **可靠性**：作為個人長時間執行的服務，Backend 應能穩定常駐運作；取樣或寫入單次失敗不應導致整個程式崩潰。
- **可攜性**：Backend 為單一可執行檔，方便部署與開機自動啟動（未來可設定為背景服務）。

---

## 5. 建議技術選型 (Tech Stack)

> 以下為達成上述需求、且對個人小專案友善的建議組合；最終仍可依實作時調整。

### Backend (Rust)

| 用途 | 建議套件 / 工具 |
|------|----------------|
| Web / API 框架 | `axum`（輕量、易學、生態完整） |
| 非同步執行環境 | `tokio` |
| 系統指標收集 | `sysinfo`（跨平台取得 CPU / RAM / 網路） |
| 資料庫 | **SQLite**（透過 `rusqlite` 或 `sqlx`，零設定、單檔、輕量） |
| 序列化 | `serde` / `serde_json` |
| CORS | `tower-http` 的 CORS 中介層 |

### Frontend

| 用途 | 建議套件 / 工具 |
|------|----------------|
| 建置工具 | **Vite** |
| 框架 | **React** |
| 樣式 | **TailwindCSS** |
| 圖表 | **Chart.js**（可搭配 `react-chartjs-2`） |
| 資料拉取 | `fetch` 或 `axios`，搭配定時器每 10 秒輪詢 |

---

## 6. 系統架構 (概念圖)

```
┌──────────────────────────────────────────────────────────┐
│                     使用者的這台機器                          │
│                                                            │
│  ┌────────────────────────┐      ┌──────────────────────┐  │
│  │   Rust Backend (binary) │      │  Frontend (Vite dev) │  │
│  │                         │      │                      │  │
│  │  每10秒取樣 ──┐          │      │  React + Tailwind     │  │
│  │  (sysinfo)   │          │      │  + Chart.js          │  │
│  │              ▼          │      │                      │  │
│  │         ┌─────────┐     │      │  每10秒 poll API      │  │
│  │         │ SQLite  │     │      │                      │  │
│  │         │ (7天)   │     │      └──────────┬───────────┘  │
│  │         └────┬────┘     │                 │              │
│  │              │          │                 │ HTTP (JSON)  │
│  │         HTTP API ◀──────┼─────────────────┘              │
│  │      /api/metrics/...   │                                │
│  └────────────────────────┘                                │
└──────────────────────────────────────────────────────────┘
        ▲                                   ▲
        │ 同一區網存取 (本機 IP)              │
   ┌─────────┐                         ┌─────────┐
   │ 本機瀏覽器│                         │ 手機/平板 │
   └─────────┘                         └─────────┘
```

---

## 7. 本版範圍界定 (Scope)

### ✅ 包含（In Scope）
- 單機 CPU / RAM / 網路三項指標的收集與呈現。
- 7 天歷史資料保留與走勢圖。
- 前後端分離；同區網裝置可存取。
- 即時數值卡片 + 歷史折線圖 + 時間範圍切換。

### ❌ 不包含（Out of Scope，未來可擴充）
- 磁碟、溫度、特定行程（process）等其他指標。
- 多主機 / agent 集中監控。
- 登入 / 密碼 / 帳號權限。
- 門檻警示、通知（Email / 推播 / 變色閃爍）。
- 公開網際網路存取與相關安全強化。

---

## 8. 後續可擴充方向 (Future Enhancements)

雖非本版需求，但設計時可預留彈性，方便日後加上：

1. **門檻警示**：CPU / RAM 超過設定值時，頁面卡片變色或發出通知。
2. **更多指標**：磁碟使用量與讀寫速度、系統溫度、各行程資源占用。
3. **多主機支援**：改為 agent 回報架構，集中監控多台機器。
4. **存取保護**：加入簡單密碼或金鑰，安全地對外網開放。
5. **部署便利化**：打包為系統服務（systemd / launchd）開機自啟。

---

## 9. 待確認 / 待辦事項

目前需求皆已確認，無未決問題。下一步可進入**實作規劃**：

1. 建立專案結構（`backend/` Rust 專案、`frontend/` Vite 專案）。
2. 定義 API 資料格式（JSON schema）。
3. 實作 Backend 取樣、儲存、清理與 API。
4. 實作 Frontend 版面、輪詢與圖表。
5. 整合測試（同區網裝置實機驗證）。
